// TODO(track-C): replace with the real bell-anchored notifications dropdown.
// This stub renders nothing so TopBar (Track B2) can mount in isolation.

interface NotificationDropdownProps {
  onClose: () => void
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function NotificationDropdown(_props: NotificationDropdownProps) {
  return null
}
